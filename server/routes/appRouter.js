// const appRouter = require('express').Router();
// const fs = require('fs').promises;

// appRouter.get('/choice', async (req, res) => {
//   res.render('choice', { title: 'Choice Page' });
// });

// module.exports = appRouter;
